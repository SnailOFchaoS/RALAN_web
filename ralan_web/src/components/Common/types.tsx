export interface InfoBlockProps{
  data?:{
    color?: string,
    text: string,
    fontSize?: number,
    blockWidth?: number,
  },
  title?:{
    color?: string,
    text: string,
    fontSize?: number,
    blockWidth?: number,
  }
}